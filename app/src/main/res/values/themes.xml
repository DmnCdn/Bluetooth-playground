<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="BaseTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="q">true</item>
    </style>
    <style name="Theme.BluetoothShowcase" parent="BaseTheme">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Customize your theme here. -->
    </style>

<!--  progress  -->

    <style name="Widget.App.LinearProgressIndicator" parent="Widget.MaterialComponents.LinearProgressIndicator">
        <item name="indicatorColor">@color/inactive_link</item>
        <item name="android:trackTint">@color/primary_color</item>
        <item name="trackThickness">4dp</item>
        <item name="trackCornerRadius">4dp</item>
    </style>

<!--  switch  -->

    <style name="Widget.App.CompoundButton.Switch" parent="">
        <item name="useMaterialThemeColors">true</item>
    </style>

    <style name="ThemeOverlay.App.CompoundButton.Switch" parent="Theme.MaterialComponents.Light">
        <item name="colorSecondary">@color/primary_color</item>
    </style>

<!--  FAB  -->

    <style name="Widget.App.FloatingActionButton" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="backgroundTint">@color/button_bg</item>
        <item name="tint">@color/button_text</item>
    </style>

    <style name="ThemeOverlay.App.FloatingActionButton" parent="">
        <item name="colorSecondary">@color/primary_color</item>
        <item name="colorOnSecondary">@color/button_text</item>
    </style>

    <style name="Widget.App.Button" parent="">
        <item name="backgroundTint">@android:color/holo_blue_dark</item>
        <item name="android:textColor">@color/button_text</item>
        <item name="cornerRadius">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:layout_height">32dp</item>
    </style>
</resources>
